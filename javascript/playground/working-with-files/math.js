/**
 * Working with External files an Es Modules
 *
 * As your Js app grows, puting all your code in one file becomes messy, Esmodules allows you to split you cod into multiple files
 *
 */

// Exporting Values

export const PI = 3.243;

export function add(a, b) {
  return a + b;
}

export function subtract(a, b) {
  return a - b;
}
